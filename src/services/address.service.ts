import { StoreAddress } from "@/types/entities";

const addressService = {
    create: async (address: StoreAddress) => {},
    update: async (address: StoreAddress) => {}
}
export default addressService